# Arby Server Configuration Template
# Copy this file to .env and fill in your values

# 📂 Folder Settings
# Local path to your PDF recipe library
PDF_FOLDER="path/to/your/recipes"

# 📧 Email Settings (For daily menus)
EMAIL_SENDER="your-email@gmail.com"
EMAIL_PASSWORD="${ARBY_EMAIL_PASSWORD}"  # Reference to system env var
EMAIL_RECEIVER="target-email@example.com"

# 🔑 Provider Keys
# It is recommended to store these in your system environment variables (e.g. ~/.zshrc)
# and reference them here using the ${VAR_NAME} syntax.
# CRITICAL: In your shell profile, use 'export' (e.g. export GEMINI_API_KEY="AIza...")
GEMINI_API_KEY="${GEMINI_API_KEY}"
OPENAI_API_KEY="${OPENAI_API_KEY}"
ANTHROPIC_API_KEY="${ANTHROPIC_API_KEY}"
XAI_API_KEY="${XAI_API_KEY}"
